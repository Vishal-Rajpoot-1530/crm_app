import 'package:flutter/material.dart';

/// Shared palette — pulled from the reference designs.
/// Keep every screen's colors deriving from here so re-theming
/// later is a one-place change.
class AppColors {
  static const bg = Color(0xFF0A0E27);
  static const cardDark = Color(0xFF12172F);
  static const cardDarker = Color(0xFF0D1226);
  static const white = Color(0xFFFFFFFF);
  static const textMuted = Color(0xFF8B92B0);
  static const textFaint = Color(0xFF5A6082);

  static const indigo = Color(0xFF4550E5);
  static const indigoLight = Color(0xFF6C7CF7);
  static const indigoPale = Color(0xFFC7CFF9);
  static const purpleIcon = Color(0xFF7C6EF2);
  static const blueIcon = Color(0xFF5B7CFA);
  static const greenIcon = Color(0xFF2FBF8F);
  static const greenAccent = Color(0xFF34C77B);
  static const cyanIcon = Color(0xFF4AC3E8);

  static const success = Color(0xFF34C77B);
  static const expired = Color(0xFFE97C7C);
  static const payment = Color(0xFF7C9CF2);

  static const slate = Color(0xFF3A4066);
}